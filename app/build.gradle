apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'Polish Language Education'
            keyPassword 'pass16384ros'
            storeFile file('D:/Android/Keystore/google_key_sore.jks')
            storePassword 'pass16384ros'
        }
    }
    compileSdkVersion 19
    buildToolsVersion "23.0.2"
    defaultConfig {
        applicationId "com.jazzros.as_languagequiz"
        minSdkVersion 14
        targetSdkVersion 19
        versionCode 101
        versionName "1.1"
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
            signingConfig signingConfigs.config
        }
    }
}

dependencies {
    compile fileTree(include: ['*.jar'], dir: 'libs')
    compile 'com.android.support:appcompat-v7:20.0.0'
}
